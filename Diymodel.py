import os
class Diymodel:
    def _init_(self,idnumber):
        self.idnumber = idnumber
    def upload(filename):
        fn = os.path.basename(filename)
        open(fn,'wb').write(filename.file.read())

